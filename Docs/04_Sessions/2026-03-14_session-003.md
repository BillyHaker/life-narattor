# Session Log — 2026-03-14 — session-003

## Meta
- Owner: Codex (GPT-5)
- Scope: Assistant / Thread isolation
- Related Skills: dev-logging-system
- Status: Done

## Goal
确保每次发给 AI 的上下文只来自当前聊天窗口，并且回包不会在切换窗口后串到别的窗口。

## Plan
1. 检查助手发请求时的上下文构造来源。
2. 把请求与回写都绑定到当前 `threadID`。
3. 验证主工程可编译。

## Work Log
1. 修改 `CaptureFeedViewModel.startAssistSessionTurn`：
   - 发送前不再直接使用全局 `assistSessionMessages`
   - 改为从当前 `threadID` 重新读取消息列表，再构造 AI 上下文
2. 修改回复整形链路：
   - `enrichAssistPayloadIfNeeded`
   - `effectiveAssistQuestionText`
   - `buildAnalyticalReply`
   - `buildReplyContract`
   均改为显式接收当前线程消息，而不是读全局消息
3. 修改回包落地逻辑：
   - AI 回复始终保存到发起请求的 `threadID`
   - 只有当前仍停留在该 thread 时，才更新界面上的 `assistSessionMessages / assistDraftPayload`

## Changes
- `Docs/05_Changes/Change-099-assist-thread-scoped-context-and-reply-binding.md`

## Verification
- `xcodebuild -project '/Users/billyha/Desktop/Life Narattor/Life Narattor.xcodeproj' -scheme 'Life Narattor' -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/life-narrator-main-derived-escalated build`

## Rollback Plan
- 回滚 `Life Narattor/ViewModels/CaptureFeedViewModel.swift` 中 thread-scoped context 构造与 thread-bound reply writeback 相关改动

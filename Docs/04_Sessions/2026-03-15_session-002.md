---
date: 2026-03-15
owner: Codex
scope: UI
status: Done
---

# Session Log

## Goal
去掉拆分页对 atom 类型的直接展示，避免兼容层粗分类干扰用户对拆分质量的判断。

## Plan
1. 移除拆分页行头的类型 icon 和类型文案。
2. 移除拆分页菜单中的“更改类型”。
3. 验证 build。

## Work Log
- 修改 `CaptureAtomRowView`，去掉左上角 `atom.type.iconName` 和 `atom.type.title`。
- 精简行内菜单，仅保留与拆分页当前使用相关的操作：添加标签、标记为重点、删除、来源。
- 保留当前底层 `AtomType` 兼容层，不影响现有其它页面依赖。

## Changes
- [Change-121-hide-atom-types-in-split-view.md](/Users/billyha/Desktop/Life%20Narattor/Docs/05_Changes/Change-121-hide-atom-types-in-split-view.md)

## Verification
- `xcodebuild -project '/Users/billyha/Desktop/Life Narattor/Life Narattor.xcodeproj' -scheme 'Life Narattor' -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/life-narrator-main-derived-escalated build` pending in this session.

## Next Notes
- 这次只改显示层，未移除底层 `AtomType`。后续若要彻底解绑，需要继续处理 `AtomDetailSheet` 与项目页等依赖。

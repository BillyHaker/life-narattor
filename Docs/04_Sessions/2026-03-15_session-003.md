---
date: 2026-03-15
owner: Codex
scope: AI/Data Model
status: Done
---

# Session Log

## Goal
做一版 `unit + attributes + tag_hints` 的拆分结构，验证拆分与标签边界是否能更清晰，而不先大改 Core Data 主模型。

## Plan
1. 将 atomize schema 从 `atoms` 改为 `record_units`。
2. 每条 unit 带上 `summary`、`attributes`、`tag_hints`。
3. 保持对旧 `atoms` 输出的兼容解码。
4. 通过兼容层继续渲染现有拆分页，先验证效果。

## Work Log
- 在 `AtomizationModels.swift` 中新增 `RecordUnitDraft` 与 `RecordUnitAttribute`。
- `AtomizeResult` 改为 `recordUnits` 主结构，同时兼容旧 `atoms` 解码。
- 将拆分显示临时映射为：`summary` 为主，必要 attributes 合并到同一条内容尾部，避免信息丢失。
- 将 app 与 backend 的 atomize schema 改为 `record_units`，并把 policy 从 `max_atoms` 改为 `max_units`。
- 更新拆分 instructions：默认少拆，仅在存在明确不同保留事项/不同阶段/不同结果时才拆开。

## Changes
- [Change-122-atomize-record-units-with-attributes-and-tag-hints.md](/Users/billyha/Desktop/Life%20Narattor/Docs/05_Changes/Change-122-atomize-record-units-with-attributes-and-tag-hints.md)

## Verification
- `node --check '/Users/billyha/Desktop/Life Narattor/server/server.js'` ✅
- `xcodebuild -project '/Users/billyha/Desktop/Life Narattor/Life Narattor.xcodeproj' -scheme 'Life Narattor' -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/life-narrator-main-derived-escalated build` pending in this session.

## Next Notes
- 这一版仍是过渡层：attributes / tag_hints 还没有完整进入独立 UI 与持久化结构。
- 若效果对路，下一步应把 tag_hints 明确接到标签候选链，而不是继续隐含在 prompt 中。

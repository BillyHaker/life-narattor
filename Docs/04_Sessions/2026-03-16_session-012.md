---
date: 2026-03-16
owner: Codex
scope: Focused Evidence Service Layer
status: Done
---

# Session Log

## Goal
把已存在的 `FocusedEvidenceBuilder` 收成统一 service 出口，避免后续搜索、专题回顾或 narrative 生成继续直接从 `NarrativeBrief` 临时拼 focused 证据。

## Plan
1. 为 `ReviewRetrievalService` 增加 focused 证据构建入口。
2. 增加基于 query 的 focused 证据构建入口，统一走 `RetrievalPlanBuilder -> MemoryIndexStore -> NarrativeBrief -> FocusedEvidenceBuilder`。
3. 增加一个轻量 focused 证据摘要文本接口，便于后续 UI 或 AI 调用。
4. 验证构建通过并记录日志。

## Work Log
- 检查了当前代码，确认 `FocusedEvidenceBuilder` 已经存在，但没有统一 service 层出口，也没有可复用的 focused 证据摘要文本接口。
- 更新 `ReviewRetrievalService`：
  - 新增 `makeFocusedEvidence(from:)`
  - 新增 `makeFocusedEvidence(query:timeRangeOverride:)`
  - 新增 `makeFocusedEvidenceText(from:)`
- 更新 `SearchScreen`：focused 问题会先展示一张“证据整理”卡，显示高频线索与证据分组摘要，再继续列出命中记录。
- 将搜索页证据分组改成可展开的 `DisclosureGroup`，可直接查看前段样本/后段样本/相关行为/相关状态的具体事项。

## Changes
- [ReviewRetrievalService.swift](/Users/billyha/Desktop/Life%20Narattor/Life%20Narattor/Data/ReviewRetrievalService.swift)
- [SearchScreen.swift](/Users/billyha/Desktop/Life%20Narattor/Life%20Narattor/Screens/SearchScreen.swift)

## Verification
1. 运行：
   - `xcodebuild -project '/Users/billyha/Desktop/Life Narattor/Life Narattor.xcodeproj' -scheme 'Life Narattor' -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/life-narrator-main-derived-escalated build`
2. 结果：构建通过。

## Next Notes
- 现在 focused 查询已经有统一 service 出口。
- 下一步若继续，应把 focused 证据层接到具体的专题/关系问题 UI 或 AI narrative 入口，而不是再从 `NarrativeBrief` 直接拼。

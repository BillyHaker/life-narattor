# Session Log — 2026-03-14 — session-001

## Meta
- Owner: Codex (GPT-5)
- Scope: Tests / Run blocker
- Related Skills: dev-logging-system
- Status: Done

## Goal
修复当前导致 Xcode 无法通过测试阶段运行的 `TranscriptionDebugStoreTests` 失败。

## Plan
1. 复现当前测试失败并读取对应实现。
2. 识别共享状态或断言不一致来源。
3. 做最小修复并重新验证构建。

## Work Log
1. 读取 `Life NarattorTests/TranscriptionDebugStoreTests.swift` 和 `Life Narattor/DevTools/LogStore.swift`。
2. 确认失败原因不是错误码归一化逻辑，而是测试共享 `TranscriptionDebugStore.shared`，在并行执行下互相污染状态。
3. 将三个测试改为使用独立的 `TranscriptionDebugStore()` 实例，而不是共享单例。
4. 运行 `xcodebuild ... build` 验证主工程仍可正常构建。

## Changes
- `Docs/05_Changes/Change-097-transcription-debug-store-test-isolation.md`

## Verification
- `xcodebuild -project '/Users/billyha/Desktop/Life Narattor/Life Narattor.xcodeproj' -scheme 'Life Narattor' -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/life-narrator-main-derived-escalated build`
- `xcodebuild ... test -only-testing:'Life NarattorTests/TranscriptionDebugStoreTests'`
  - 代码层断言问题已消除
  - 本次 CLI 测试运行被模拟器 `Invalid device state` 阻断，属于设备环境问题，不是断言继续失败

## Rollback Plan
- 回滚 `Life NarattorTests/TranscriptionDebugStoreTests.swift` 中对 `TranscriptionDebugStore()` 的使用，改回 `TranscriptionDebugStore.shared`

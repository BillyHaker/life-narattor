# Session Log

- Date: 2026-04-19
- Owner: Codex
- Scope: UI / Beta polish
- Related: `ContentView.swift`, beta privacy intro
- Status: In Progress

## Goal
- Make the first-launch privacy intro feel like a natural app entry step instead of a bottom sheet interruption.

## Plan
- Replace the current sheet-based privacy intro with a first-launch full-screen intro screen.
- Keep the existing `AppStorage` flag so the intro only appears once.
- Preserve the same privacy boundary messaging while improving hierarchy and flow.

## Work Log
- Diagnosed the current issue: the privacy intro is presented as a bottom sheet attached to the home UI, which makes it feel like a late interruption instead of a natural beginning.

## Changes
- Replaced the first-launch privacy intro bottom sheet with a full-screen intro screen rendered directly from `ContentView`.
- Preserved the one-time `AppStorage` gate and the same privacy messaging, but moved it into a more natural first-run entry flow.

## Verification
- `xcodebuild -project '/Users/billyha/Desktop/Life Narattor/Life Narattor.xcodeproj' -scheme 'Life Narattor' -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/life-narrator-main-derived-escalated build`
  - blocked by local simulator runtime availability (`No available simulator runtimes for platform iphonesimulator`)
- `xcodebuild -project '/Users/billyha/Desktop/Life Narattor/Life Narattor.xcodeproj' -scheme 'Life Narattor' -destination 'generic/platform=iOS' -derivedDataPath /tmp/life-narrator-main-derived-device build`
  - blocked by local signing/provisioning profile availability

## Next Verification
- Open the app from Xcode.
- Clear `app.hasSeenPrivacyIntro` if needed.
- Confirm first launch now shows a full-screen intro instead of a bottom sheet.
- Tap `继续` and confirm the app enters the main tab UI naturally.


## Additional Goal
- Make assistant voice input feel like a single focused recording state instead of a stacked overlay that repeats or obscures text.

## Additional Plan
- Replace the current stacked recording UI + text composer with a mutually exclusive recording mode.
- Refine the recording UI so it reads like a dedicated control card with clearer hierarchy.
- Keep the existing recording/transcription pipeline unchanged.

## Additional Work Log
- Diagnosed that the assistant screen rendered both `RecordingChipView` and `CaptureInputBarView` at the same time during recording, which created the cluttered bottom area and made message text feel visually duplicated.

## Additional Changes
- Updated `RecordFeedScreen` so the bottom inset now shows either the recording UI or the normal input bar, never both.
- Redesigned `RecordingChipView` into a clearer recording card with status copy, timer, meter bars, and more distinct stop/cancel actions.

## Additional Verification
- `xcodebuild -project '/Users/billyha/Desktop/Life Narattor/Life Narattor.xcodeproj' -scheme 'Life Narattor' -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/life-narrator-main-derived-escalated build`
  - blocked by local simulator runtime availability (`No available simulator runtimes for platform iphonesimulator`)
- `xcodebuild -project '/Users/billyha/Desktop/Life Narattor/Life Narattor.xcodeproj' -scheme 'Life Narattor' -destination 'generic/platform=iOS' -derivedDataPath /tmp/life-narrator-main-derived-device build`
  - blocked by local signing / provisioning profile availability when targeting generic iOS devices

## Additional Next Verification
- Open `记录 -> 助手` from Xcode.
- Start a voice recording and confirm the bottom area switches into a dedicated recording state instead of stacking with the normal composer.
- Confirm message content remains readable while recording.
- Tap `停止` and verify transcription still returns to the current assistant thread.

## Additional Goal 2
- Remove explanatory copy from the assistant recording card so the UI feels more like a system recording state and less like an inline tutorial.

## Additional Plan 2
- Remove the long helper sentence from `RecordingChipView`.
- Keep only the recording status, timer, waveform, and actions.
- Preserve the current recording layout and logic.

## Additional Work Log 2
- Confirmed the biggest remaining downgrade in the new recording card was the helper sentence under `正在录音`, which made the control read like a product tutorial instead of a focused recording state.

## Additional Changes 2
- Removed the secondary explanatory sentence from `RecordingChipView`.
- Kept the recording UI structure intact so this is a copy-only refinement.

## Additional Verification 2
- `xcodebuild -project '/Users/billyha/Desktop/Life Narattor/Life Narattor.xcodeproj' -scheme 'Life Narattor' -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/life-narrator-main-derived-escalated build`
  - blocked by local simulator runtime availability (`No available simulator runtimes for platform iphonesimulator`)
- `xcodebuild -project '/Users/billyha/Desktop/Life Narattor/Life Narattor.xcodeproj' -scheme 'Life Narattor' -destination 'generic/platform=iOS' -derivedDataPath /tmp/life-narrator-main-derived-device build`
  - blocked by local signing / provisioning profile availability when targeting generic iOS devices

## Additional Goal 3
- Add lightweight `编辑` and `删除` actions to the capture detail screen so a record can be adjusted or removed without backing out to the list.

## Additional Plan 3
- Add two small record-level actions to the detail screen toolbar.
- Make edit use a small text editing sheet that updates the current saved body while keeping original input as provenance.
- Make delete use a confirmation dialog and remove related atoms/artifacts together with the capture.
- Refresh the record list immediately after edit/delete.

## Additional Work Log 3
- Confirmed the current detail screen had no record-level edit/delete entry; the only delete action in this view was for atom rows.
- Chose a conservative edit model: update the saved record body, keep original input, and clear stale split/tag artifacts so later processing can regenerate from the new body.

## Additional Changes 3
- Added `编辑` and `删除` actions to the top-right area of `CaptureDetailSheet`.
- Added a lightweight `CaptureEditSheet` for editing the current saved record body.
- Added destructive delete confirmation for the whole capture.
- Added cleanup for related atoms, atom-tag links, and artifacts when editing or deleting.
- Added a callback from `CaptureDetailSheet` back to `RecordFeedScreen` so the list refreshes immediately after edit/delete.

## Additional Verification 3
- `xcodebuild -project '/Users/billyha/Desktop/Life Narattor/Life Narattor.xcodeproj' -scheme 'Life Narattor' -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/life-narrator-main-derived-escalated build`
  - blocked by local simulator runtime availability (`No available simulator runtimes for platform iphonesimulator`)
- `xcodebuild -project '/Users/billyha/Desktop/Life Narattor/Life Narattor.xcodeproj' -scheme 'Life Narattor' -destination 'generic/platform=iOS' -derivedDataPath /tmp/life-narrator-main-derived-device build`
  - blocked by local signing / provisioning profile availability when targeting generic iOS devices

## Additional Next Verification 3
- Open `记录`, tap any record card, and enter the detail screen.
- Confirm there are lightweight `编辑` and `删除` actions in the detail navigation area.
- Tap `编辑`, update the body text, save, and confirm the detail content updates and the list refreshes.
- Tap `删除`, confirm the destructive dialog, and verify the detail closes and the record disappears from the list.

## Additional Goal 4
- Make the new detail-level `编辑 / 删除` entry feel lighter and more native to an iOS detail page.

## Additional Plan 4
- Keep `编辑` as the visible common action.
- Move destructive `删除` into an overflow menu to reduce visual weight.
- Preserve the existing edit sheet and delete confirmation logic.

## Additional Work Log 4
- The first pass exposed both `编辑` and `删除` directly in the top-right area, which was functionally correct but still felt too management-heavy for a reading-focused detail page.

## Additional Changes 4
- Kept `编辑` as the single visible toolbar action.
- Moved `删除记录` into an `ellipsis.circle` overflow menu.
- Preserved the underlying edit and delete flows unchanged.

## Additional Goal 5
- Restore direct visibility for the destructive action while keeping it visually lighter than the original first pass.

## Additional Plan 5
- Bring `删除` back out of the overflow menu.
- Keep it visible beside `编辑`, but tone it down visually so it still feels like a secondary destructive action.
- Leave the confirmation dialog unchanged.

## Additional Work Log 5
- The overflow-menu version looked lighter, but it added one extra tap to a destructive action the user explicitly wanted to keep directly visible.

## Additional Changes 5
- Restored `删除` as a directly visible toolbar action beside `编辑`.
- Kept the styling lighter by using a softer destructive text treatment instead of a heavier button presentation.

## Additional Goal 6
- Add a lightweight `隐藏` action to capture detail so a record can be removed from the feed without removing its split artifacts or analysis participation.

## Additional Plan 6
- Add an `isHiddenFromFeed` flag to `CaptureEntity` and the programmatic Core Data model.
- Filter hidden captures out of the record feed only.
- Add a visible `隐藏` action beside `编辑 / 删除` in the detail toolbar.
- Keep atoms, artifacts, tags, and retrieval participation intact.

## Additional Work Log 6
- Confirmed the right implementation boundary is UI-level hiding rather than deletion: the user wants records out of the feed, but still available to downstream AI review and retrieval.
- Reused the existing detail-level action pattern instead of adding a separate management screen.

## Additional Changes 6
- Added `isHiddenFromFeed` to `CaptureEntity` and to the programmatic Core Data model with a default value of `false`.
- Updated feed loading to exclude hidden captures while leaving the rest of the persistence and analysis chain intact.
- Added a direct `隐藏` action to `CaptureDetailSheet` that saves the hidden flag, refreshes the list, and dismisses the detail view without deleting atoms or artifacts.

## Additional Verification 6
- `xcodebuild -project '/Users/billyha/Desktop/Life Narattor/Life Narattor.xcodeproj' -scheme 'Life Narattor' -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/life-narrator-main-derived-escalated build`
  - blocked by local simulator runtime availability (`No available simulator runtimes for platform iphonesimulator`)
- `xcodebuild -project '/Users/billyha/Desktop/Life Narattor/Life Narattor.xcodeproj' -scheme 'Life Narattor' -destination 'generic/platform=iOS' -derivedDataPath /tmp/life-narrator-main-derived-device build`
  - blocked by local signing / provisioning profile availability when targeting generic iOS devices

## Additional Next Verification 6
- Open `记录`, enter any record detail page, and confirm `编辑 / 隐藏 / 删除` are visible in the top-right area.
- Tap `隐藏` and confirm the detail closes and the record disappears from the feed.
- Ask an AI review question that should still surface hidden-record content and confirm it can still contribute to analysis.
- Re-open another record and confirm `删除` still performs a true destructive delete.

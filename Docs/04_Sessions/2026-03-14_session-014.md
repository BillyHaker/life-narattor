# Session 014 - 2026-03-14

## Summary
- Fixed backend atomization path so record splitting now uses the AI proxy instead of the old local punctuation stub.
- Added backend `/v1/atomize` route and wired `BackendAIService.atomize(...)` to it.
- This resolves the mismatch where chat/transcription used backend AI but splitting still used local fallback logic.

## Files Changed
- server/server.js
- Life Narattor/AI/AIService.swift

## Verification Steps
- `node --check server/server.js`
- Code inspection of `BackendAIService.atomize(...)` and new `/v1/atomize` route

## Verification Notes
- `server.js` syntax check passed.
- Full `xcodebuild` remains blocked by the local Xcode Observation macro environment in existing DevTools files.

## Rollback Notes
- Restore the previous local `BackendAIService.atomize(...)` stub if backend atomize routing needs to be disabled.
- Remove `/v1/atomize` from the proxy if you need to revert to direct/local atomization behavior.

## Follow-up
- Updated manual retry so tapping `重新拆分` triggers immediate atomization instead of only re-queuing `pendingSplit`.
- Added capture processing state notifications so the detail sheet refreshes live instead of holding the original snapshot state.

# Session Log — 2026-03-14 — session-010

## Meta
- Owner: Codex (GPT-5)
- Scope: Assistant / Archive UX
- Related Skills: dev-logging-system
- Status: Done

## Goal
让“整理为记录”总结整个当前窗口，并将确认卡按钮从“重开会话”改为“重新整理”。

## Plan
1. 把归档生成输入从“最后一条用户消息”改成“整个窗口总结任务”。
2. 让归档上下文使用当前窗口全量消息。
3. 将确认卡的次操作改为“重新整理”。
4. 验证工程可编译。

## Work Log
1. 在 `CaptureFeedViewModel` 中新增：
   - `regenerateAssistDraft()`
   - `archiveQuestionText(from:)`
2. 更新 `generateAssistDraftForActiveThread(...)`：
   - `questionText` 改为基于整窗用户消息拼出的总结任务
   - `contextText` 改为使用当前窗口全量消息，而不是默认最近 8 条
3. 更新 `RecordFeedScreen`：
   - `AssistDraftConfirmCard` 的次按钮改为“重新整理”
   - 按钮行为改为重新生成当前窗口的归档草稿
4. 运行 `xcodebuild ... build`

## Changes
- `Docs/05_Changes/Change-106-assist-archive-summarize-full-window-and-regenerate.md`

## Verification
- `xcodebuild -project '/Users/billyha/Desktop/Life Narattor/Life Narattor.xcodeproj' -scheme 'Life Narattor' -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/life-narrator-main-derived-escalated build`

## Rollback Plan
- 回滚：
  - `Life Narattor/ViewModels/CaptureFeedViewModel.swift`
  - `Life Narattor/Screens/RecordFeedScreen.swift`

## Update - AI review tab simplification
- Removed the ReviewHome entry from the main tab flow.
- The `回顾` tab now opens `SearchScreen()` directly inside a `NavigationStack`.
- This removes the weekly/monthly review entry points from the main user path and matches the current requirement: manual-input AI review only.
- Validation: `xcodebuild -project '/Users/billyha/Desktop/Life Narattor/Life Narattor.xcodeproj' -scheme 'Life Narattor' -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/life-narrator-main-derived-escalated build` => `EXIT:0`
## Update - overview time-query intent fix
- Fixed `RetrievalPlanBuilder` so broad review questions like `我过去一周主要发生了什么变化` are treated as `openReview/overview`, not `comparison`.
- Previous behavior misclassified these queries because they contained `变化`, which pushed them into focused comparison mode without anchors or tag matches; retrieval then scored everything to 0 and returned the false `no records` state.
- Validation: `xcodebuild -project '/Users/billyha/Desktop/Life Narattor/Life Narattor.xcodeproj' -scheme 'Life Narattor' -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/life-narrator-main-derived-escalated build` => `EXIT:0`
## Update - overview AI analysis in AI review screen
- Fixed `SearchScreen` so overview/open-review queries also request and render AI analysis.
- Previously only focused evidence flows (`comparison` / `relation`) showed an `AI 分析` card; overview queries only rendered related records.
- Added overview analysis state and request path using `ReviewRetrievalService.makeNarrativeMaterial(...)` + `AIService.analyzeNarrativeMaterial(...)`.
- Validation: `xcodebuild -project '/Users/billyha/Desktop/Life Narattor/Life Narattor.xcodeproj' -scheme 'Life Narattor' -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/life-narrator-main-derived-escalated build` => `EXIT:0`
## Update - AI review output reframed as facts and links
- Reworked overview and focused AI analysis prompts away from long report style.
- New target structure: `事实：` + `联系：` + `可继续问：`.
- The review UI now labels the top card as `事实与联系` instead of generic `AI 分析`.
- Validation: `node --check '/Users/billyha/Desktop/Life Narattor/server/server.js'` and `xcodebuild -project '/Users/billyha/Desktop/Life Narattor/Life Narattor.xcodeproj' -scheme 'Life Narattor' -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/life-narrator-main-derived-escalated build` => pass / `EXIT:0`

## 20:10 Assistant Voice Input Fix
- Root cause: assist surface reused the shared microphone entry, but transcribed voice captures in `mode = assist` were only persisted as captures and never bridged into `startAssistSessionTurn(...)`.
- Added `sourceThreadID` to `VoiceCaptureDraft` so assistant voice captures keep their originating thread.
- On transcription completion for `mode = assist`, the capture now stops at `cleanReady` instead of entering normal auto-split, then immediately sends the cleaned transcript into the active assist thread.
- Added a lightweight notice: `语音转写完成，已发送给助手`.
- Build verification: `xcodebuild ... build` => `EXIT:0`.

## 20:30 AI Review Follow-up Layer
- SearchScreen updated so evidence groups are no longer auto-expanded; `expandedEvidenceGroupIDs` now starts empty.
- Added a lightweight follow-up layer under the current AI review result.
- Follow-up questions now stay bound to the current overview `NarrativeMaterial` or focused `FocusedEvidenceBundle` instead of re-running global retrieval.
- Added suggestion chips for follow-up questions and inline follow-up Q/A cards.
- Extended AI analysis endpoints to accept optional `followup_question` for both focused evidence and overview narrative material.
- Build verification: `node --check server/server.js` passed; `xcodebuild ... build` => `EXIT:0`.

## 20:45 AI Review Follow-up Bubbles
- Replaced follow-up result cards in `SearchScreen` with chat-like user/assistant bubbles.
- Initial review answer now also uses the same assistant bubble style for visual consistency.
- Goal: keep deeper AI review interaction readable without turning the page into stacked analysis cards.
- Build verification: `xcodebuild ... build` => `EXIT:0`.

## 20:48 AI Review First-Answer Readability
- Broke first AI review answer rendering into section blocks when it contains `事实： / 联系： / 可继续问：` labels.
- This only changes presentation, not retrieval or answer generation.
- Goal: make the first answer easier to scan while keeping the chat-bubble visual language.
- Build verification: `xcodebuild ... build` => `EXIT:0`.

## SearchScreen follow-up UX refinement
- Made AI review follow-up suggestion chips context-aware instead of fixed.
- Overview chips now derive from `primaryThemes`, `changeSignals`, and `repeatedPatterns` when available.
- Focused chips now derive from `focusAnchor`, `relationAnchors`, top signals, and evidence group titles.
- Added follow-up collapse strategy: only the latest two Q/A rounds are shown by default, with a toggle to expand earlier rounds.
- Reset follow-up collapse state on new retrieval runs and after appending a new follow-up answer.

## AI Review starter prompt auto-search fix
- Fixed starter prompt taps in AI Review so they no longer get cleared by the `query` on-change reset path.
- Added a short-lived suppression flag for programmatic query updates that should immediately trigger retrieval.
- Starter prompt taps now reliably fill the input and launch analysis in one action.

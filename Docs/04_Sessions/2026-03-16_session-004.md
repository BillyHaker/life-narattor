---
date: 2026-03-16
owner: Codex
scope: Tag Suggestion UI Boundary
status: Done
---

# Session Log

## Goal
把标签建议的用户可见边界做清楚：用户需要区分“复用已有显性标签的推荐”和“新标签候选”，同时保持“显性标签只来自用户主动输入或接受推荐”。

## Plan
1. 为 `TagItem` 增加显性/隐藏可见性信息。
2. 在详情页区分两种建议标签文案和样式。
3. 保持确认逻辑不变：点击建议标签即接受。
4. 验证构建。

## Work Log
- 为 `TagItem` 增加 `isUserVisible` 字段。
- 为建议标签增加 UI 文案区分：
  - 已有显性标签推荐：`推荐`
  - 新标签候选：`新建议`
- 详情页标签胶囊颜色也做了区分，新标签候选使用更偏蓝的底色，减少和已有显性标签建议混淆。
- 同步补齐 `AtomTagStore.swift`、`SearchScreen.swift`、`ProjectDetailScreen.swift` 中的 `TagItem` 构造。

## Changes
- [Change-131-distinguish-existing-tag-recommendations-vs-new-candidates.md](/Users/billyha/Desktop/Life%20Narattor/Docs/05_Changes/Change-131-distinguish-existing-tag-recommendations-vs-new-candidates.md)

## Verification
- `xcodebuild -project '/Users/billyha/Desktop/Life Narattor/Life Narattor.xcodeproj' -scheme 'Life Narattor' -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/life-narrator-main-derived-escalated build` ✅

## Next Notes
- 当前详情页已能表达推荐来源差异。
- 后续如果继续做标签体验，可考虑在接受时加一句极轻的确认反馈，例如“已加入显性标签库”仅在新标签候选场景出现。

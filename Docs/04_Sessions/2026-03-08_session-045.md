# Session Log — 2026-03-08 — session-045

## Meta
- Owner: Codex (GPT-5)
- Scope: iOS/AssistUX/Prompting
- Related Skills: capture-ui, dev-logging-system
- Status: Done

## Goal
降低助手回复的模板感，让默认首答更像真实个人助手：短、自然、连贯、可马上执行。

## Plan
1. 改 app 侧与 proxy 侧的 assist 系统提示词，约束默认输出风格。
2. 改本地 enrich/fallback 逻辑，移除固定 `Why/How/下一步` 模板。
3. 保留发音场景 IPA 能力，并将动作建议改为最小可执行步骤。
4. 编译验证并记录日志。

## Work Log
1. 在 `AIService.swift` 与 `server/server.js` 更新 assistant 指令:
   - 默认首答 3-5 句自然中文。
   - 非用户明确要求时不使用固定编号结构。
   - 每轮只推进一个小动作并最多一个关键追问。
2. 在 `CaptureFeedViewModel.swift` 重写本地 enrich 逻辑:
   - 模板检测 (`looksTemplatedReply`)。
   - 纯复述检测 (`looksLikeParaphraseOnly`)。
   - 默认短回复 + 上下文衔接 (`conversationBridgePrefix`)。
   - 仅在用户明确要详细方案时走详细模式 (`wantsDetailedPlan`)。
3. 保留并强化发音场景细节:
   - `fan /fæn/`、`fine /faɪn/` 音标提示。
   - 30-60 秒最小动作练习与单一关键追问。
4. 编译验证:
   - 普通沙箱构建出现 `swift-plugin-server` / `sandbox-exec` 干扰。
   - 升级到提权构建后确认 `BUILD SUCCEEDED`。

## Decisions
- 本次无需新增 ADR（仅属提示词与回复策略调优，无数据契约变更）。

## Changes
- `Docs/05_Changes/Change-092-assist-destyle-short-natural-reply.md`

## Verification
- `xcodebuild -project '/Users/billyha/Desktop/Life Narattor/Life Narattor.xcodeproj' -scheme 'Life Narattor' -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/life-narrator-main-derived-escalated build`
  - Result: `BUILD SUCCEEDED`
- `node --check /Users/billyha/Desktop/Life Narattor/server/server.js`
  - Result: pass

## Failures / Dead Ends
- 在受限沙箱中构建出现宏插件与 sandbox 噪声错误，无法作为有效代码回归依据。
- 通过提权单次构建拿到稳定结果，确认本轮改动可编译。

## Next Handoff Notes
- 建议用户重点手测一轮短输入场景: “我总把 fan 和 fine 说混了。”
- 关注项: 首答是否仍出现固定模板编号；是否单轮只问一个关键问题。

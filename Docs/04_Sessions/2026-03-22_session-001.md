# 2026-03-22 Session 001

## Scope
- Add a lightweight beta admin dashboard.
- Add invite generation, send, list, resend, and beta registration endpoints.
- Expose usage summaries suitable for beta operations.

## Work Completed
- Added admin HTML routes at `/admin`, `/admin/users`, `/admin/users/:id`, and `/admin/invites`.
- Added lightweight admin access control via `ADMIN_TOKEN` when configured.
- Added invite persistence in `server/invite_store.js` with states for created, sent, used, revoked, and send failures.
- Added `POST /admin/invites` to generate and send invite codes and `POST /admin/invites/resend` to resend existing invites.
- Added `POST /v1/beta/register` so invite codes can mint stable beta `user_id` identities.
- Expanded beta-user storage helpers to support listing users and invite-based registration, including optional Apple subject binding.
- Expanded usage storage helpers so the admin dashboard can render daily totals, per-user summaries, recent events, and quota-hit visibility.
- Added Resend-based email sending support for invites when `RESEND_API_KEY` and `INVITE_EMAIL_FROM` are configured; otherwise invites still generate and record a clear unsent state.
- Verified the admin dashboard and beta registration flow with a localhost smoke test.

## Files Changed
- `server/server.js`
- `server/beta_user_store.js`
- `server/usage_limits.js`
- `server/invite_store.js`
- `Docs/04_Sessions/2026-03-22_session-001.md`
- `Docs/05_Changes/Change-167-admin-dashboard-and-invite-registration.md`

## Verification
- `node --check server/server.js`
- `node --check server/invite_store.js`
- `xcodebuild -project '/Users/billyha/Desktop/Life Narattor/Life Narattor.xcodeproj' -scheme 'Life Narattor' -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/life-narrator-main-derived-escalated build`
- Localhost smoke test:
  - `GET /admin?token=test-admin` renders dashboard
  - `POST /admin/invites` generates an invite and records it
  - `POST /v1/beta/register` returns a stable `user_id`

## Rollback Notes
- Remove `server/invite_store.js` and revert admin/invite/register routes in `server/server.js`.
- Revert the user/usage helper expansions in `server/beta_user_store.js` and `server/usage_limits.js`.
- No app schema changes or destructive record rewrites are involved.

## Launchd environment loading fix
- Diagnosed the local admin/backend startup issue as a launchd runtime permissions problem: the agent could start `server.js`, but failed when trying to `source` the `.env` file from the Desktop workspace at runtime.
- Updated `manage_launchd_proxy.sh` so install-time now loads `.env` once and writes the needed variables into the launchd plist `EnvironmentVariables` block.
- Removed runtime dependence on `source .env` from the launchd command itself.
- Rotated launchd stdout/stderr logs during reinstall so old `.env`/cwd errors no longer pollute current diagnostics.
- Reinstalled the agent and confirmed logs are now clean: the proxy starts and `/healthz` responds without `.env` errors.

## Beta preflight checklist consolidation
- Re-read the earlier beta launch checklist and identity/privacy design draft.
- Consolidated them into a tighter preflight checklist aimed at App Review / test build readiness rather than general roadmap planning.
- Split remaining work into three buckets: must finish before submission, should finish if time allows, and explicitly hide/defer for this beta.
- Captured the current intended beta surface: AI Review as the main review entry, assistant draft-before-commit flow, and hidden old review/search entry points.

## Beta preflight round 1
- Verified the current beta surface against code paths instead of assumptions.
- Confirmed that AI Review is the only review tab entry in the main TabView, while weekly/monthly review screens remain outside the tester-facing main path.
- Confirmed DevTools remain DEBUG-only and that assistant archive now uses the fullscreen draft editor before commit.
- Updated the preflight checklist to mark those boundary items as complete.
- Added four new verification backlog items for the remaining manual beta checks: AI Review flow, assistant draft commit flow, assistant voice return-to-thread, and tag/hidden-tag validation.

## VRF-013 AI review readability pass
- Tightened the AI Review first-answer rendering so `事实 / 联系 / 可继续问` display as shorter section blocks instead of one dense assistant bubble.
- Added lightweight expand / collapse behavior for long `事实` and `联系` sections to keep the first screen scannable.
- Rendered `可继续问` inside the first answer as tappable follow-up prompts so the transition from reading to asking the next question is more direct.
- Kept verification conservative: build passed, but the backlog item remains pending until a manual 2-3 round follow-up check is completed in app.

## VRF-014 Assistant draft confirmation polish
- Polished the fullscreen draft editor so the primary action now clearly communicates that the draft will be written into records.
- Added lightweight draft meta pills for record-unit count, tag suggestion count, and confidence, so the user can read what kind of draft they are reviewing before committing.
- Added a post-confirm transient notice to make it clearer that the record has been written and that split/tag processing will continue afterward.
- Kept this pass intentionally small and UI-focused; the backlog item remains pending until the full manual path is walked in app.

## VRF-016 Tag suggestion CTA clarity
- Reworked the record detail tag suggestion presentation so suggested tags no longer appear as ordinary pills mixed into the normal tag list.
- Added a dedicated `推荐保留的标签` card inside atom rows with explicit guidance telling the user to choose tags they want to keep for long-term use.
- Changed each suggested tag into a lightweight row with a single clear action: `保留`.
- Split atom tags into two layers: `推荐保留的标签` for pending suggestions and `已保留标签` for confirmed tags.
- Added a lightweight confirmation notice after accepting a suggested tag so the user understands it has been kept for future reuse.
- Kept this pass focused on clarity and CTA wording; no tag schema or suggestion-generation logic changed.

## Files Changed
- `Life Narattor/Views/CaptureDetailSheet.swift`
- `Docs/04_Sessions/2026-03-22_session-001.md`
- `Docs/05_Changes/Change-174-tag-suggestion-cta-clarity.md`

## Verification
- `xcodebuild -project '/Users/billyha/Desktop/Life Narattor/Life Narattor.xcodeproj' -scheme 'Life Narattor' -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/life-narrator-main-derived-escalated build`

## Rollback Notes
- Revert the tag suggestion panel and notice changes in `Life Narattor/Views/CaptureDetailSheet.swift`.
- No schema, persistence, or backend behavior changed in this pass.

## VRF-016 Unified suggested tag picker
- Removed per-atom suggested-tag panels from the split view so recommended tags no longer repeat once for every record unit.
- Added a single record-level suggested-tag entry card that summarizes how many recommended tags are available and opens one unified selection sheet.
- Added a unified `推荐标签` sheet with an explicit close action (`完成`) and default sheet drag-to-dismiss behavior so the user can stay, select multiple tags, and exit on their own timing.
- Kept keep-actions non-destructive to the sheet itself: selecting `保留` updates the underlying suggestion state and keeps the sheet open for further choices.

## Files Changed
- `Life Narattor/Views/CaptureDetailSheet.swift`
- `Docs/04_Sessions/2026-03-22_session-001.md`
- `Docs/05_Changes/Change-175-unified-tag-recommendation-sheet.md`

## Verification
- `xcodebuild -project '/Users/billyha/Desktop/Life Narattor/Life Narattor.xcodeproj' -scheme 'Life Narattor' -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/life-narrator-main-derived-escalated build`

## Rollback Notes
- Revert the unified suggested-tag entry card and sheet in `Life Narattor/Views/CaptureDetailSheet.swift`.
- No schema or persistence changes are involved in this pass.

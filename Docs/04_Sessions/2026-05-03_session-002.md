# 2026-05-03 Session 002

## Metadata
- Date: 2026-05-03
- Owner: Codex
- Scope: Product/Backend/UX
- Status: Done
- Related skills: None

## Goal
Defer paid subscription work and align the app/backend around one public free tier with a monthly AI usage limit.

## Plan
1. Change backend default public usage tier from `trial` to `free`.
2. Keep internal override tiers available for reviewer/manual testing, but remove active paid-tier wording from current docs and UI.
3. Update Settings copy to explain the current free plan and monthly free AI quota.
4. Improve user-facing HTTP 402 quota-exhaustion messages for record splitting, assist archive, and transcription.
5. Verify backend default tier/exhaustion behavior, Node syntax, Swift build, and stale user-facing copy.
6. Update verification backlog and commit.

## Files changed
- `server/usage_limits.js`
- `server/.env.example`
- `server/README.md`
- `Life Narattor/Screens/AppSettingsScreen.swift`
- `Life Narattor/ViewModels/CaptureFeedViewModel.swift`
- `Docs/03_Decisions/ADR-016-free-tier-only-launch.md`
- `Docs/04_Sessions/2026-05-03_session-002.md`
- `Docs/05_Changes/Change-230-free-tier-only-launch.md`
- `Docs/VERIFICATION_BACKLOG.md`

## Implementation
- Changed backend fallback `USAGE_DEFAULT_TIER` from `trial` to `free`.
- Updated `.env.example` so new deployments default to `free`.
- Rewrote backend usage docs to describe `free` as the current public default and `trial/daily/deep` as internal override tiers only.
- Updated Settings to show `免费版`, `每月免费额度`, and a clear note that paid subscriptions are not currently open.
- Removed the visible `7 天试用` pill from Settings.
- Added user-friendly HTTP 402 copy for free AI quota exhaustion in atomization, assist archive, and transcription flows.

## Decisions
- [ADR-016 - Free Tier Only Launch](../03_Decisions/ADR-016-free-tier-only-launch.md)

## Changes
- [Change 230 - Free Tier Only Launch](../05_Changes/Change-230-free-tier-only-launch.md)

## Verification
- `node --check server/usage_limits.js && node --check server/server.js` passed.
- Backend smoke test with a temporary usage store confirmed a fresh user resolves to `free`, receives a 300-credit monthly limit, and returns `ai_credit_exhausted` when exhausted.
- `git diff --check` passed.
- Xcode MCP `BuildProject` passed for `windowtab1`.
- `rg` found no stale `7 天试用`, `免费体验`, `管理订阅`, `¥8`, or `¥16` wording in app and server files.

## Manual verification
- Deploy backend with no `USAGE_DEFAULT_TIER` override and persistent `USAGE_STORE_PATH`; confirm new users show `free` in `/admin/users/<id>`.
- Temporarily set `USAGE_CREDIT_LIMIT_OVERRIDES={"free":3}` in a staging backend and confirm the app shows the friendly free-quota exhaustion copy.
- Open Settings in the app and confirm the plan card and AI section communicate free-tier-only behavior.

## Rollback
- Revert this commit to restore automatic public trial behavior and the prior Settings copy.
- If only backend behavior needs rollback, set `USAGE_DEFAULT_TIER=trial` in the backend environment.

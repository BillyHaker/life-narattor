# Session Log — 2026-03-07 — session-012

## Meta
- Owner: Codex (GPT-5)
- Scope: Voice/Recording/Transcription/UI/Docs
- Related Skills: capture-ui, error-handling-standard, dev-logging-system
- Status: Done

## Goal
Investigate and fix two regressions found in manual test:
1) recording duration stuck at `00:00`;
2) transcription failed after recording.

## Starting Context
- User reported timer not moving and transcription failure.
- Recent changes had introduced real speech transcription path.

## Plan
1. Inspect recording timer rendering path in UI.
2. Inspect transcription failure path in view model and service.
3. Apply minimal fixes and keep behavior explicit for permissions.
4. Build in isolated path and document rollback.

## Work Log (chronological)
1. Reviewed `RecordingChipView`; found elapsed time computed from `Date()` but no periodic re-render trigger.
2. Reviewed transcription path in `CaptureFeedViewModel` and `VoiceTranscriptionService`; identified permission-denied path could fail after recording without early user guidance.
3. Applied fixes:
   - Converted recording chip to `TimelineView(.periodic)` for per-second UI updates.
   - Added speech authorization preflight before starting recording.
   - Added dedicated speech-permission alert in `RecordFeedScreen`.
   - Added runtime fallback alert when transcription throws `permissionDenied`.
4. Built in isolated path and confirmed compile success.

## Failed Attempts / Dead Ends
- None.

## Decisions (link ADRs)
- No ADR required; no schema/API/privacy contract changes.

## Changes (link ChangeLogs)
- `Docs/05_Changes/Change-059-recording-timer-and-speech-permission-fix.md`

## Verification
- Build:
  - `xcodebuild -project '/private/tmp/life-narrator-codex-fix/Life Narattor.xcodeproj' -scheme 'Life Narattor' -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/life-narrator-worktree-derived build`
  - Result: `EXIT:0`

## Rollback Plan
- Revert:
  - `Life Narattor/Views/RecordingChipView.swift`
  - `Life Narattor/VoiceTranscriptionService.swift`
  - `Life Narattor/ViewModels/CaptureFeedViewModel.swift`
  - `Life Narattor/Screens/RecordFeedScreen.swift`
  - `Docs/04_Sessions/2026-03-07_session-012.md`
  - `Docs/05_Changes/Change-059-recording-timer-and-speech-permission-fix.md`
- Rebuild using the same verification command.

## Next Steps / Handoff Notes
- Manual test should confirm timer increments every second and permission failure is now actionable (settings deep link).

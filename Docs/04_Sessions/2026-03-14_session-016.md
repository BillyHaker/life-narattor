---
date: 2026-03-14
owner: Codex
scope: Build/DevTools/Tests
related_skills:
  - dev-logging-system
status: Done
---

# Session Log

## Goal
修复本轮新增改动后出现的 build failed 问题，并处理截图中的编译/测试阻塞项。

## Work Log
- 将 `Notification.Name.captureProcessingStateChanged` 从 `private` 调整为可跨文件访问。
- 为 `TranscriptionDebugStoreTests` 增加显式清理，避免测试读到残余状态。
- 将 `FeatureFlags`、`LogStore`、`TranscriptionDebugStore`、`AIDebugStore` 从 `@Observable` 宏迁移为 `ObservableObject`，绕过本机 Xcode 宏插件故障。
- 将 `CaptureFeedViewModel` 从 `@Observable` 迁移为 `ObservableObject`，并同步 `RecordFeedScreen` 的持有方式改为 `@StateObject`。
- 将 `#Preview` 宏替换为 `PreviewProvider`，避免 `PreviewsMacros` 构建报错。
- 重新运行 `xcodebuild`，确认构建通过。

## Changes
- [Change-113-build-fix-remove-observation-and-preview-macro-dependency.md](/Users/billyha/Desktop/Life%20Narattor/Docs/05_Changes/Change-113-build-fix-remove-observation-and-preview-macro-dependency.md)

## Verification
- `xcodebuild -project '/Users/billyha/Desktop/Life Narattor/Life Narattor.xcodeproj' -scheme 'Life Narattor' -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/life-narrator-main-derived-escalated build` ✅ `EXIT:0`

## Next Notes
- 建议在 Xcode 里再跑一次对应测试 target，确认 `TranscriptionDebugStoreTests` 现在也恢复稳定。

## Follow-up
- 继续诊断用户现场出现的 atomize `HTTP400`。
- 发现高风险点：OpenAI 直连 atomize schema 比 backend 更严格且不一致，可能直接触发 400。
- 将 OpenAI 直连 `atomizeSchema()` 收敛为与 backend 同构：`confidence/start_char/end_char` 支持 null 且列入 required，`atomize_version` 也列入 required。
- 重新运行 `xcodebuild`，确认 `EXIT:0`。

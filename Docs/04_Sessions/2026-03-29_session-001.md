# 2026-03-29 Session 001

## Scope
- Remove third-person system wording from assistant-to-record draft generation.
- Make archive drafts read like direct personal record drafts instead of case summaries.

## Work Completed
- Tightened the `assistArchive` prompt in both app and backend so generated draft titles/body must avoid product-summary wording such as `用户`, `助手`, `AI`, `系统`, `总结`, `摘要`, and `纪要`.
- Shifted the writing target toward natural Chinese record language that can be saved directly.
- Added a lightweight local normalization pass in `CaptureFeedViewModel` to sanitize archive titles, context, and record-unit summaries if the model still slips into third-person summary wording.
- Kept the archive flow unchanged: fullscreen draft editor, light editing, and confirm-before-commit still work the same.

## Files Changed
- `Life Narattor/AI/AIService.swift`
- `server/server.js`
- `Life Narattor/ViewModels/CaptureFeedViewModel.swift`
- `Docs/04_Sessions/2026-03-29_session-001.md`
- `Docs/05_Changes/Change-177-assist-draft-natural-record-language.md`

## Verification
- `node --check 'server/server.js'`
- `xcodebuild -project '/Users/billyha/Desktop/Life Narattor/Life Narattor.xcodeproj' -scheme 'Life Narattor' -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/life-narrator-main-derived-escalated build`
- Manual path:
  1. Open the app and go to 记录 -> 助手.
  2. Have a short supplement-related conversation.
  3. Tap `整理为记录`.
  4. Confirm the draft title/body no longer use `用户` / `助手` wording and read like a direct record draft.

## Rollback Notes
- Revert the assist archive prompt updates in `AIService.swift` and `server.js`.
- Revert the local normalization helpers in `CaptureFeedViewModel.swift` if the wording cleanup proves too aggressive.
- No schema or persistence changes are involved in this pass.

---

## Additional Work Completed
- Switched the backend default transcription provider from OpenAI to Doubao for beta usage.
- Kept explicit provider override support through `TRANSCRIBE_PROVIDER`.
- Improved the Doubao missing-config error so local setup failures are easier to understand.

## Additional Files Changed
- `server/server.js`
- `Docs/05_Changes/Change-178-switch-default-transcription-to-doubao.md`

## Additional Verification
- `node --check 'server/server.js'`
- `xcodebuild -project '/Users/billyha/Desktop/Life Narattor/Life Narattor.xcodeproj' -scheme 'Life Narattor' -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/life-narrator-main-derived-escalated build`

## Additional Rollback Notes
- Revert `server/server.js` to restore OpenAI as the default transcription provider.

# Session Log — 2026-03-14 — session-008

## Meta
- Owner: Codex (GPT-5)
- Scope: Assistant / Rendering
- Related Skills: dev-logging-system
- Status: Done

## Goal
去掉聊天回复的字符裁剪，并让助手消息支持 Markdown 显示。

## Plan
1. 移除聊天回复路径中的 `limitReplyLength(...)`
2. 将助手消息气泡从纯文本渲染改为 Markdown 渲染
3. 验证工程可编译

## Work Log
1. 更新 `CaptureFeedViewModel.normalizeAssistReply`：
   - 去掉 `limitReplyLength(normalized, maxChars: 220)`
2. 更新 `RecordFeedScreen.AssistSessionBubble`：
   - 助手消息优先用 `AttributedString(markdown:)`
   - 失败时回退到纯文本显示
3. 运行 `xcodebuild ... build`

## Changes
- `Docs/05_Changes/Change-104-assist-no-length-clipping-and-markdown-bubble.md`

## Verification
- `xcodebuild -project '/Users/billyha/Desktop/Life Narattor/Life Narattor.xcodeproj' -scheme 'Life Narattor' -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/life-narrator-main-derived-escalated build`

## Rollback Plan
- 回滚：
  - `Life Narattor/ViewModels/CaptureFeedViewModel.swift`
  - `Life Narattor/Screens/RecordFeedScreen.swift`
